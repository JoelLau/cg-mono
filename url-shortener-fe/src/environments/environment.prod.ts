import { EnvironmentVariables } from './environment.types';

export const environment: EnvironmentVariables = {
  production: true,
  api_base_path: 'http://localhost:3000', // TODO: replace with production URL
};
